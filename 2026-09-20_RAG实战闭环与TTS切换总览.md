# RAG 实战闭环与 TTS 切换总览 — 2026-09-19/20

> **TL;DR**: 2 天内完成 4 个大闭环：① OCR review 5 文件 + Top 6 真 bug 修复 ② Jev-Like 决策 API 集成到 RAG (Router/Verifier/Citation) ③ 增量更新教程 ④ **TTS backend 切换 mmx → edge-tts**（最新）。

---

## 1. 全闭环流程图

```
[对话输入 chat_outbox.txt]
        ↓ STT GUI 拾音 (Vosk 中文)
[Mavis 处理]
        ↓ 生成回复 + 调用 Jev-Like 做 Router/Verifier/Citation
[scripts/rag_engineering/jev_like.py] — 决策 API
        ↓ json_schema strict → 0 类型错误
[生成回复文字]
        ↓ 调用 tts_edge.py (edge-tts 微软免费)
[tts_inbox/reply_YYYYMMDD_HHMMSS.mp3]
        ↓ atomic write + 时间戳清理
[daemon pygame 播放]
        ↓ 用户听到 (理论上应该听 1 条音频)
[麦克风拾音] ← 回音问题待修 (方案 A/B/C/D 待选)
```

---

## 2. 4 个闭环详情

### 2.1 OCR Code Review + Top 6 修复 ✅

**触发**: 集成 Jev-Like 改 5 个核心文件（jev_like.py / router.py / verifier.py / hallucination_defense.py / pipeline.py）

**流程**:
```
.\run_ocr_review.ps1 -Path .  (PowerShell + WorkingDirectory 切中文根)
  ↓ 6.6min 跑完 179 LLM 请求
OCR log 末尾有完整 findings
```

**Top 6 真 bug** (已修):

| 文件:行 | severity | 摘要 | 修法 |
|---|---|---|---|
| `rag/jev_like.py:324-355` | bug·medium | `extra_questions` 数据丢失 | 加 `extras` 字段 + parser 提取 |
| `rag/verifier.py:266-269` | bug·critical | `previous_facts=[]` NameError | 加 `c_contradiction_conf` 变量 |
| `rag/pipeline.py:46` (OCR 误判) → 实际 `build_index.py:130` | bug·high | `load_docs_from_dir` 缺 `include_exts` | 加白名单 `{".md", ".txt", ...}` |
| `data/.ragignore` | security·high | `.env/` 不匹配 `.env` 文件 | 加 `.env` + 7 个新模式 |
| `rag/jev_like.py:210-220` | bug·medium | API key + base_url 错配 | 分离 env_key + base_url |
| `rag/jev_like.py` L20/30/33/76 | maintainability·low | 4 处 unused imports / unused logger / unused self.timeout | 清理 + `_timeout` 改名 |

**测试结果**:
- ✅ 12/12 unit test (`test_fix_top6.py`)
- ✅ 25/25 `tests/test_loader.py`
- ✅ 20/20 `tests/test_comprehensive.py` (5 层 × 4 项 = 20 个场景)

### 2.2 Jev-Like 决策 API 集成 RAG ✅

**已写入**: `data/rag_engineering/2026-09-20_RAG集成JevLike决策教程.md` (17.4 KB / 39 chunks)

**集成 3 个组件**:
- Router: `rag/router.py:_decide_via_jev_like` (single_hop/multi_hop 决策)
- Verifier: `rag/verifier.py:_verify_via_jev_like` (矛盾/充分性/幻觉 3 项)
- Citation: `rag/hallucination_defense.py:JevLikeCitationChecker`

**3 级 fallback**: Jev-Like → LLM (mmx) → 启发式
**3 个开关**: `use_jev_like_router/verifier/citation` (pipeline 默认 False, 显式开启)

### 2.3 RAG 增量更新教程 ✅

**已写入**: `data/rag_engineering/2026-09-20_RAG增量更新实战教程.md` (12 KB / 23 chunks)

**核心教训**: **不要**直接调 `build_index.py --mode incremental`（伪增量，仍全量 re-embed 980 chunks ~15min）。**总是用 `scripts/import_doc.py`**（自动调 `incremental_build.py` 复用旧 vec + 只 embed 改动文件）。

**3 种典型场景**: 加新文件 / 改旧文件 (mtime 自动) / 删旧文件
**性能**: rag_engineering 473 chunks full=4min vs incremental=8s（30x）

### 2.4 TTS backend 切换: mmx → edge-tts ✅ **(2026-09-20 最新)**

**触发**: mmx Token Plan 配额限流 (间歇 401/1004) 持续 1.5+ 小时

**新默认脚本**: `C:\Users\Administrator\Desktop\minimax\tts_edge.py`

**关键设计**:
- **edge-tts** 微软 Edge TTS，完全免费，0 token 限流
- **atomic write**: 写 `.tts_*.tmp` → rename (Windows NTFS rename 是原子)
- **严格时间戳清理**: 只清 `reply_YYYYMMDD[_|-]HHMMSS.mp3`，**保留**带 label 的（`reply_alpha_xxx.mp3`）
- **positional 参数**传中文（PowerShell 引号 bug workaround）
- 9 个 voice（6 中文 + 3 英文），默认 `zh-CN-XiaoxiaoNeural`

**3 个 backend 对比**:

| Backend | 价格 | Token限流 | 中文质量 | 当前可用 |
|---|---|---|---|---|
| **edge-tts** (新默认) | 完全免费 | **0** | ⭐⭐⭐⭐ | ✅ |
| mmx (speech-2.8-hd) | Token Plan 配额 | 401/1004 间歇 | ⭐⭐⭐⭐⭐ | ⚠️ 间歇 |
| SiliconFlow CosyVoice2 | 促销免费 | key 401 当前 | ⭐⭐⭐ | ❌ SF key 失效 |

**用法**:
```powershell
# 默认 (推荐 positional)
python tts_edge.py '回复文本内容'

# 改 voice
python tts_edge.py '你好' --voice zh-CN-YunjianNeural

# 列 voice
python tts_edge.py --list-voices

# 跳过清理 (保留历史 reply)
python tts_edge.py '你好' --no-clean
```

---

## 3. 待修问题

### 3.1 回音问题 (回音循环 self-talk)

**症状**: daemon 播放 TTS → mic 拾取 → STT 又识别 → 又触发回复 → 又播

**根因**: `real_time_stt_gui.py` 无 VAD (静音检测), 一直在拾音

**方案** (待选):
| 方案 | 改 daemon | 改 STT GUI | 效果 |
|---|---|---|---|
| A. 音量减半 | 1 行 | 0 | echo 弱 50%, 体验也降 |
| B. 信号文件 mute | 3 行 | 5 行 | 完全静音 mic, 不影响体验 |
| C. 文本 echo cancel | 5 行 | 15 行 | 兼容 mic 没静音情况 |
| D. STT 加 VAD | 0 | ~30 行 | 最彻底 (任何 TTS 源都防) |

**当前未修**, 等用户选定方案后实施。

### 3.2 OCR review cosmetic findings (低优先级)

剩余 1-2 个 cosmetic finding (low maintainability):
- `rag/router.py:256` — `_JevRouteResult` dataclass 定义在函数内部
- 其他: 注释 / 命名优化

**停止准则**: 改完一轮修复后再 review, 剩下的 finding 都是 cosmetic 时可以停 (实测 7 轮 review 后 cosmetic 收敛)

---

## 4. 关键工程教训 (2026-09-19/20 新增)

### 4.1 OCR review 不是一次性的
- 修复一轮后再 review, 每次都会暴露新 bug
- 实测 pipeline.py (387 行): 第 1 轮 3 findings → 第 5 轮 28 findings 累计
- **停止准则**: finding 数稳定在 ~3 个 cosmetic 时停
- Token Plan 消耗: 5 轮 review ~250K tokens, 5h 窗口足够

### 4.2 TTS 切换 + 限流应对
- 单一 backend (mmx) 不可靠, 备 2-3 个 fallback
- atomic write + 时间戳清理 = 避免连播多个音频
- daemon 的 mtime float precision race 已用 atomic write 大幅缓解 (但不能完全消除)

### 4.3 导入文档标准流程
**踩坑**: 写完教程/文档后直接写到 `data/<kb>/` 跳过 wrapper 是错误的 (用户纠正 2026-09-20 02:10)

**标准流程**:
1. 先写到桌面 `My reliable experience\<file>.md` (源)
2. 跑 `python scripts/import_doc.py '<desktop_path>' --kb <kb>` 自动 5 步
3. wrapper 自动: 收集源 → copy 到 KB → 更新 00_index.md → incremental_build → recall 验证 (top-1 必须命中)

**修复方案** (跳过 wrapper 后):
1. `Copy-Item <kb_file> '<desktop>\My reliable experience\<file>'`
2. `mavis-trash <kb_file>` (不用 Remove-Item)
3. `python scripts/import_doc.py '<desktop>\My reliable experience\<file>' --kb <kb>`

### 4.4 PowerShell 子进程 buffer 陷阱
- 长跑 python 子进程用 `python | Tee-Object` 会缓冲所有 stdout 到 EOF
- 正确做法: `Start-Process -WindowStyle Hidden` + RedirectStandardOutput 到 log 文件 + 轮询日志大小
- 或者: `python -u` flag 启动 (unbuffered)

---

## 5. 当前工程状态

| 维度 | 数据 |
|---|---|
| RAG 系统 | 5 文件修复 ✅, 45/45 测试通过 ✅ |
| Jev-Like 集成 | Router/Verifier/Citation ✅, 3 开关待生产开 |
| KB 索引 | rag_engineering 496 chunks (4.7 MB), recall 0.889 |
| TTS backend | edge-tts 默认 ✅, daemon alive, atomic write |
| 待修 | 回音方案选择, cosmetic OCR findings |
| 待办 | decision_api.py HTTP 服务, OCR CI gate, SF key 等待恢复 |

---

## 6. 相关文档 (已入 KB)

- `2026-09-18_RAG系统体检+修复实战总结.md` — 22 条踩坑 + 体检修复
- `2026-09-20_RAG集成JevLike决策教程.md` — Router/Verifier/Citation 集成
- `2026-09-20_RAG增量更新实战教程.md` — 增量更新实战
- `RAG实战踩坑22条结构化总结_2026-09-18.md` — 22 条结构化
- `Jev_Like_国内可行方案_SiliconFlow_Qwen_2026-09-19.md` — SF + Qwen 方案
- `Jev_TypeSafe_SystemOne_Model_2026-09-15.md` — Jev 原版调研

**底层工具源码**:
- `scripts/import_doc.py` — 标准化导入 wrapper
- `scripts/incremental_build.py` — 真增量引擎
- `scripts/tts_sf.py` — SF TTS (备用, SF key 当前失效)
- `C:\Users\Administrator\Desktop\minimax\tts_edge.py` — edge-tts 默认

---

**结论**: 2026-09-19/20 完成 RAG 实战闭环 + TTS 切换。所有改动有 OCR review + 端到端测试 + 增量教程。下一步等用户确认回音修法 + 跑生产 benchmark。