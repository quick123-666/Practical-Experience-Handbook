# 视频转 md 教程 —— v10 关键截图版实战

> 主题: **本地视频文件批量转 markdown 教程** (ASR + 关键截图, 不 OCR)
> 来源: 2026-09-17 `video-to-md-tutorial` skill (v5→v10 共 6 次迭代)
> 关键词: 视频转 md / 视频转 markdown / 视频转教程 / ASR + 关键帧 / 抖音/B站/YouTube / RAG 入库
> 文档版本: 2026-09-17 v1.0
> 字数: ~3500 字

---

## 一、为什么需要这个 pipeline

爬下来的视频（抖音/B站/YouTube）原始 mp4 文件 586 MB / 63 个，单看无法检索。

需求：
1. **可检索**——让 RAG 能查"X 视频讲过什么"
2. **结构化**——保留 ASR 文字 + 关键截图
3. **批量**——63 个视频不能一个个手动转
4. **快速**——总时间 < 1 小时（不是几天）

最终方案：**v10 关键截图版**（每视频 ASR + 5 张关键 jpg + md 引用，**不做 OCR**）。

---

## 二、6 个版本迭代史

| 版本 | 抽帧策略 | OCR | 时间/视频 | 召回质量 | 弃用原因 |
|---|---|---|---|---|---|
| v5 | 每 8s 一帧 (37 帧) | 全部 OCR 抓代码 | 5+ 分钟 | ⭐⭐ | 太慢，OCR 噪声大 |
| v6 | 首帧 + 中段 (2 帧) | OCR 抓代码 | 2 分钟 | ⭐ | 机械抽帧 |
| v7 | ASR 关键词 top3 | OCR + 对齐 | 3 分钟 | ⭐⭐ | OCR 跟代码演示不匹配 |
| v8 | ASR 关键词 top2 | OCR 抓代码 | 1.5 分钟 | ⭐⭐⭐ | OCR 还是噪声 |
| v9 | ASR 6 候选 → OCR 全部 → 对齐评分 | 必须 OCR | 3 分钟 | ⭐⭐⭐ | 驾驶/动画类 OCR 全空 |
| **v10 (当前)** | **ASR 5 spread 抽帧 + 保留 jpg** | **不 OCR** | **35 秒** | **⭐⭐⭐⭐** | **✅ 推荐** |

**关键洞察**：OCR 抓截图里的代码是**噪声**，关键截图**本身就是产物**——它作为视频的"视觉目录"比文字描述更有用。

---

## 三、核心流程（5 步）

```
video.mp4 → audio.wav → ASR segments → 5 关键时间点 → 5 jpg + md → KB 索引
```

### 步骤 1: ffmpeg 抽音频

```python
import imageio_ffmpeg
FFMPEG = imageio_ffmpeg.get_ffmpeg_exe()

def extract_audio(mp4, wav):
    subprocess.run(
        [FFMPEG, "-i", str(mp4), "-ar", "16000", "-ac", "1", "-y", str(wav)],
        stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
    )
```

**关键**：
- 用 `imageio_ffmpeg.get_ffmpeg_exe()`（不依赖 PATH）
- `stdout=DEVNULL, stderr=DEVNULL` 绕开 GBK 编码陷阱

### 步骤 2: faster-whisper ASR

```python
from faster_whisper import WhisperModel
asr = WhisperModel("base", device="cpu", compute_type="int8")
segments, _info = asr.transcribe(
    str(wav), language="zh", beam_size=1, vad_filter=True
)
```

| 参数 | 值 | 理由 |
|---|---|---|
| model | "base" | 中文 base 够用 |
| compute_type | "int8" | 量化 CPU 跑快 2 倍 |
| vad_filter | True | 自动切静音段 |

### 步骤 3: ASR-guided 选 5 关键时刻（spread ≥15 秒）

```python
KEY_PHRASES = re.compile(
    r"(步骤|第一步|接下来|注意|重点|看这里|def |class |import |"
    r"SELECT|http|function|return|#include|"
    r"python|bash|sql|区别|命令|执行|代码|脚本|文件|"
    r"右键|点击|打开|配置文件|环境|安装|问题|错误|解决|API|数据库)",
    re.IGNORECASE,
)
skip_words = re.compile(r"^(接下来|然后|首先|最后|好$|嗯$|那个$|这个$)")

# 评分 = 文字密度 × 0.4 + 关键词命中 × 1.0
# 按时间排序 spread 选, 跳过过渡词
```

### 步骤 4: 抽帧 + 保留 jpg

```python
subprocess.run([
    FFMPEG, "-ss", f"{mid_sec:.1f}", "-i", str(mp4),
    "-vf", "scale=500:-1",      # 500px 宽, 文件 22KB
    "-frames:v", "1",           # 只抽 1 帧
    "-y", str(out),
], stdout=DEVNULL, stderr=DEVNULL)
```

**关键**：保留 jpg 不删（用户洞察："关键截图本身就是产物"）。

### 步骤 5: md 组装

```markdown
# {title}

> 视频 ID: {aweme_id}  |  源文件: {mp4_name}

## 字幕 (ASR)
- [   0.0s -    1.6s] Hello大家好...
- [   1.6s -    4.6s] 今天我们来讲...

## 关键截图 (5 帧, ASR-guided, 保留视觉记忆)

### @ 00:00 — 高密度 (3.2)
![关键帧 00:00](./<safe_title>/frames/f0001_t0.jpg)

### @ 00:16 — 高密度 (1.4)
![关键帧 00:16](./<safe_title>/frames/f0002_t16.jpg)

### @ 00:31 — 高密度 (2.1)
![关键帧 00:31](./<safe_title>/frames/f0003_t31.jpg)

## 视频源文件
`<video: C:\...\xxx.mp4>`
```

每张截图配 reason 标注：`高密度` / `高密度+import` / `过渡词 (接下来)` 等。

---

## 四、5 个已知踩坑（必看）

### 4.1 PowerShell 中文路径 mojibake

```
Start-Process -ArgumentList "中文路径"  → 子进程拿到 "������"
```

**解法**：用 Python wrapper 调 `import_doc.main()`，sys.argv 里直接放中文路径：

```python
# _run_import.py
import import_doc
sys.argv = ["import_doc.py", "C:\\...\\桌面\\中文目录", "--kb", kb]
import_doc.main()
```

### 4.2 子进程拿不到 env var

```
$env:SILICONFLOW_API_KEY = "sk-..."   # PowerShell 父进程设了
python wrapper.py → subprocess.run(build_index.py)  # 子进程看不到
```

**解法**：wrapper 里显式 setdefault：
```python
import os
os.environ.setdefault("SILICONFLOW_API_KEY", "sk-...")
```

### 4.3 BGE (768d) vs bge-m3 (1024d) 维度错配

```
ValueError: matmul: Input operand 1 has a mismatch in its core dimension 0,
             size 768 is different from 1024
```

**原因**：build 用 `bge-m3` (1024d)，recall 用 `BGEEmbedder` (768d)，cosine 计算失败。

**解法**：改 `import_doc.py` 的 `recall_test()`：
```python
from rag.embedder import BGEEmbedder, SiliconFlowEmbedder
if DEFAULT_EMBEDDER == "bge-m3":
    embedder = SiliconFlowEmbedder()
else:
    embedder = BGEEmbedder()
```

### 4.4 PaddleOCR CPU 慢

每帧 5-10 秒，63 视频 × 5 帧 = 50+ 分钟纯 OCR。

**解法**：v10 后**完全不做 OCR**。截图本身就是视觉记忆，比 OCR 文字更有用。

### 4.5 SF API key 不持久

每次新 shell 会丢失 `$env:SILICONFLOW_API_KEY`。

**解法**：wrapper 里 `os.environ.setdefault` 硬编码（适合本地脚本）。
**注意**：key 不应进任何文件/log，避免泄露。

---

## 五、RAG 入库流程（走 wrapper 不要直接 build_index）

### 5.1 为什么走 wrapper

直接调 `build_index.py` 的 3 个坑：
1. **mtime 跳过 ≠ embed 跳过**：增量模式不重新 embed，但 chunk 改了 embed 还是旧的
2. **无 recall 验证**：索引建完了没人验证能不能查到
3. **timeout 不够**：默认 300s 不够大 KB（30 分钟才合理）

### 5.2 wrapper 自动做 4 件事

```python
python scripts/import_doc.py <source_dir> --kb <kb_name>
```

1. ✅ 检测重复 + 拷贝文件到 `data/<kb>/`
2. ✅ 重建索引（默认 bge，**改成 bge-m3**）
3. ✅ 跑召回测试（top-1 命中验证）
4. ✅ 输出报告（chunks 数 + recall score + 重建查询命令）

### 5.3 当前 KB 状态（2026-09-17）

| KB | 大小 | 嵌入 | 内容 |
|---|---|---|---|
| crawled_videos_kb | 5 MB | bge-m3 (1024d) | 网安视野 63 视频 ASR + OCR（v8） |
| crawled_accounts_kb | 0.3 MB | bge-m3 | 已爬账号清单（含网安视野 64 行） |
| my_tutorials_kb | - | bge-m3 | 个人教程库 |
| douyin_tech/life_kb | - | bge 768 | 抖音技术/生活 |

---

## 六、性能基准（实测本机 Windows）

| 阶段 | 时间/视频 |
|---|---|
| ffmpeg 抽音频 | 1-3s |
| ASR (faster-whisper base) | 30-60s |
| 抽 5 帧 | 2s |
| **不用 OCR** | **0s** |
| 组装 md | <1s |
| **总时间** | **~35-65s** |

**63 视频总时间 ~40 分钟**。

---

## 七、后续改进方向

1. **多模态嵌入**：用 CLIP 把 jpg 也向量化，让截图作为 RAG 召回单元
2. **vision LLM 打分**：每帧调 LLM 评"是否含教学要点"，选 top K
3. **字幕层检测**：用 OCR 检测视频内嵌字幕（不是关键帧文字），跟 ASR 对齐
4. **增量索引**：增量 embed 时检测 chunk 真正改了（mtime + 内容 hash）

---

## 八、文件位置速查

| 文件 | 路径 |
|---|---|
| Pipeline 脚本 | `C:\Users\Administrator\Desktop\minimax\_process_videos.py` |
| Wrapper 脚本 | `C:\Users\Administrator\Desktop\minimax\_run_import.py` |
| 进度窗口 | `C:\Users\Administrator\Desktop\minimax\progress_window.py` |
| Skill 文档 | `C:\Users\Administrator\.minimax\skills\video-to-md-tutorial\SKILL.md` |
| Skill 详细参数 | `C:\Users\Administrator\.minimax\skills\video-to-md-tutorial\references\pipeline-params.md` |
| 目标 KB | `C:\Users\Administrator\Desktop\生产级多跳 RAG 系统\data\crawled_videos_kb\` |