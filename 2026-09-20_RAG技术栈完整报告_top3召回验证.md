# RAG 技术栈完整报告 — 2026-09-20 (基于 KB 召回 top-3)

> 整理时间: 2026-09-20 20:30
> 触发 query: "我们是如何配置 Jev 的"
> RAG 召回: TOP-3 全相关 (confidence 0.57)
> 用途: 增量灌入 rag_engineering KB, 让 Mavis 自己 query "我们的技术栈是什么" 时直接命中

---

## 0. 召回证据

```
Q> 我们是如何配置 Jev 的
[fallback] LLM=mock 未生成有意义的答案, dump 检索 top-3 文档
[置信度] 0.57
[耗时]   581ms
[Embedder] sf-bge-m3 + zh-1 query prefix (默认)

TOP-1: 2026-09-20_Anil-matcha_awesome-jev-by-typesafe_README_2026-09-20.md
TOP-2: Jev_TypeSafe_SystemOne_Model_2026-09-15.md
TOP-3: 2026-09-20_RAG集成JevLike决策教程.md
```

---

## 1. Jev 真模型 (我们没用, 但 KB 要有)

| 项 | 详情 |
|---|---|
| 公司 | TypeSafe AI（旧金山, 2026-09-15 发布） |
| 创始人 | Diogo Almeida（前 OpenAI, 参与发明 RLHF + ChatGPT） |
| 融资 | $40M 种子（DCVC 领投） |
| 模型类别 | **System One Model**（不是 LLM） |
| 训练方法 | RLCD (Reinforcement Learning for Calibrated Decisions) |
| 训练数据 | 完全合成数据（TypeSafe 自建数据实验室） |
| **状态** | 闭源 + waitlist + 出境合规 → 我们用 Jev-Like 国内替代 |

## 2. Jev vs LLM 性能 (TypeSafe 官方)

| 维度 | 现有 LLMs | System One + Jev |
|---|---|---|
| 优化算法 | RLHF / RLVR | **RLCD** |
| 输出 | 字符串 | **类型安全结构化值** (schema, 0 类型错误) |
| 采样 | 顺序生成 | **并行**（硬件友好） |
| 延迟 | 3-329 秒 | **70-500ms** |
| 价格 | $0.20-$10/MTok | **$0.042/MTok + 输出免费** |
| 结构化错误率 | 0.58-45.5% | **0%** |

**Headline 数字**: 193.6x 更快, 444.6x 更便宜。

## 3. Jev 三原语

| 原语 | 用途 | 返回 |
|---|---|---|
| **Choice** | 从预定义选项选一个 | `{"billing":0.08,"technical":0.85,"sales":0.07}` + confidence |
| **Score** | 按等级打分 | `"urgency": 0.92` + probability |
| **Noul** | 是/否概率 | `"is_spam": 0.87` + confidence |

每输出都**附带校准过的置信度**（high conf → high accuracy）。

---

## 4. 我们的 Jev-Like 国内替代 (实测跑通)

### 4.1 配置栈

| 项 | 值 |
|---|---|
| Provider | SiliconFlow (`api.siliconflow.cn/v1`) |
| 模型 | `Qwen/Qwen2.5-7B-Instruct`（免费） |
| SDK | `openai>=1.0`（兼容模式调 SF） |
| 关键开关 | `response_format.type="json_schema"` + `strict=True` |
| 环境变量 | `SILICONFLOW_API_KEY` (新 key 已 verify4 端点 200) |

### 4.2 完整代码模板

```python
import sys, json, os
from openai import OpenAI

sys.stdout.reconfigure(encoding='utf-8')
client = OpenAI(
    base_url='https://api.siliconflow.cn/v1',
    api_key=os.environ['SILICONFLOW_API_KEY'],
)

# === 模仿 Jev 的 Choice + Noul 原语 ===
schema = {
    'type': 'object',
    'properties': {
        'department': {
            'type': 'object',
            'properties': {
                'choice': {'type': 'string', 'enum': ['billing', 'technical', 'sales']},
                'probabilities': {
                    'type': 'object',
                    'properties': {
                        'billing': {'type': 'number'},
                        'technical': {'type': 'number'},
                        'sales': {'type': 'number'},
                    },
                    'required': ['billing', 'technical', 'sales'],
                },
                'confidence': {'type': 'number'},
            },
            'required': ['choice', 'probabilities', 'confidence'],
        },
        'is_urgent': {'type': 'number'},
    },
    'required': ['department', 'is_urgent'],
}

state = '我被多收了一笔费用, 请帮我退款, 很急!'
resp = client.chat.completions.create(
    model='Qwen/Qwen2.5-7B-Instruct',
    messages=[
        {'role': 'system', 'content': '你是 Jev-like 决策模型. 返回带概率的 JSON 决策.'},
        {'role': 'user', 'content': f'STATE: {state}\n\n按 schema 返回决策.'},
    ],
    response_format={
        'type': 'json_schema',
        'json_schema': {'name': 'decision', 'schema': schema, 'strict': True},
    },
)
```

**关键**: `strict: True` → schema 数学保证字段类型正确 → 模拟 Jev 的 0 类型错误。

---

## 5. RAG 集成 (4 处替换)

### 5.1 Router 替换

```python
from rag.router import Router
router = Router(llm=your_llm, use_jev_like=True)
decision = router.decide("张三的报销流程")
# 自动走 Jev-Like 路由, 失败 fallback 到 LLM
```

改动: `Router.__init__` 加 `use_jev_like=False`, 新方法 `_decide_via_jev_like` (89 行).

### 5.2 Verifier 替换

```python
verifier = Verifier(use_jev_like=True)
result = verifier._verify_via_jev_like(
    sub_question_text="什么是异化劳动",
    retrieved_chunks=["异化劳动是指...", "马克思在..."],
    previous_facts=["马克思是《资本论》作者."],
)
# {"contradiction_prob":0.05, "sufficient_prob":0.85, "hallucination_prob":0.10, "confidence":0.93}
```

### 5.3 Citation Check 替换

```python
from rag.hallucination_defense import JevLikeCitationChecker
checker = JevLikeCitationChecker(support_threshold=0.6, no_support_threshold=0.4)
result = checker.check(claim="...", citation="...")
# {"supports": True, "verdict": "supports", "support_prob": 1.00, "confidence": 0.99}
```

### 5.4 Pipeline 集成 (主入口)

```python
rag = MultiHopRAG(
    llm=llm, embedder=embedder, store=store,
    use_jev_like_router=True,       # Router 用 Jev-Like 替代 LLM
    use_jev_like_verifier=True,     # Verifier 用 Jev-Like 替代 LLM
    use_jev_like_citation=True,     # 启用 JevLikeCitationChecker
)
```

---

## 6. Hybrid Decision Router (jev_router.py)

| 类别 | Backend | 理由 |
|---|---|---|
| intent / route | **M3** (minimax) | M3 推理稳定 (100% vs Jev 80%) |
| risk / severity | **Jev** (Qwen) | Jev 省配额 + 不 None |
| noul | **dual** (M3 + Jev 双校验) | 两边都弱, 抓不一致 |
| 默认 | M3 | 保守起见 |

**A/B 测评**: M3 13/23 (57%), Jev 11/23 (48%), 差距 9 个百分点

入口:
- CLI: `python jev_router.py <category> <input>`
- Python: `from jev_router import smart_route; smart_route("intent", "...")`

---

## 7. RAG Embedder 配置 (今天刚改)

**默认 SiliconFlowEmbedder + BGE-ZH v1.5 官方 query prefix**:

```python
DEFAULT_QUERY_PREFIX = "为这个句子生成表示以用于检索相关文章："

def as_query_encoder(self):
    self._is_query_hint = True
    if self._query_prefix is None:
        self._query_prefix = self.DEFAULT_QUERY_PREFIX
    return self
```

`MultiHopRAG.__init__` 自动 `embedder.as_query_encoder()` → 全 RAG 查询走 SF bge-m3 + zh-1 prefix。

**Recall 效果** (5 query 实测):
- token 256-dim: T1=3/5, T3=5/5
- bge-m3 raw: T1=2/5, T3=4/5
- **bge-m3 + zh-1 prefix: T1=3/5, T3=4/5** ← 已落地, 追平 token TOP-1

---

## 8. 工程栈总览 (基于今天 24h)

| 层 | 技术 |
|---|---|
| **LLM** | M3 (minimax) + Jev (SF Qwen2.5-7B) + dual |
| **RAG** | 4-stage multi-hop, vector store, hybrid retriever |
| **Embedder** | SF bge-m3 (1024-dim) + zh-1 query prefix, token 256-dim fallback |
| **JSON Schema** | strict mode (OpenAI 兼容 → SF) 模拟 Jev 0 类型错误 |
| **Code Review** | OpenCodeReview (OCR) provider minimax-cn + MiniMax-M3 |
| **TTS** | edge-tts (zh-CN-XiaoxiaoNeural, 完全免费) |
| **Audio** | tts_daemon.py (watermark 模式) + sentinel 防护回音 |
| **STT** | Vosk (中文, 离线) + RMS VAD + daemon-playing gate |
| **Memory** | Mavis hybrid decision router + topic memory + SkillClaw 蒸馏 |

---

## 9. KB 当前状态

```
data/rag_engineering/
├── 33 .md (5 Anil-matcha awesome-jev + 5 Jev + 教程 + 本报告)
├── index_sf_bge_m3.pkl (3.7 MB / 831 chunks / 1024-dim) ← 默认
├── index_token.pkl     (1.3 MB / 831 chunks / 256-dim)  ← offline fallback
└── _index_mtime.json   (mtime cache for incremental build)
```

---

## 10. 一句话总结

我们的 RAG 系统是一个 **LLM (M3) + Jev-Like 决策模型 (SF+Qwen) + BGE 中文检索 + OCR 代码审查** 的多 Agent 工程栈, 通过 `MultiHopRAG` 统一编排, **用真 Jev 思路但用国产模型实现**, 部署在中国大陆可用的 SF API 上。

---

> 字数: ~1500 字
> 灌入路径: 桌面 My reliable experience → import_doc.py --kb rag_engineering → incremental_build.py (默认) → recall 验证 top-1 命中