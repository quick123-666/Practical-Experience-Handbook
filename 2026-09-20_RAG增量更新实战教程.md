# RAG 增量更新实战教程 — 2026-09-20

> **TL;DR**: 用 `import_doc.py` 走标准流程（拷文件 + 增量重建索引 + 召回验证）。**不要**直接调 `build_index.py --mode incremental`（伪增量，会全量 re-embed）。**真增量**用 `scripts/incremental_build.py`（`import_doc.py` 已自动调它）。

---

## 1. 为什么需要增量更新

RAG 系统跑起来后，**文件会持续变化**：
- 加新教程（每日/每周）
- 改旧文件（错别字/补充内容）
- 删废弃文档
- 批量爬新数据

**全量 rebuild 的代价**（vs 增量）：

| KB 规模 | 全量 rebuild | 增量 rebuild | 倍数 |
|---|---|---|---|
| 100 chunks | 30s | 2s | 15x |
| 500 chunks | 2.5 min | 5s | 30x |
| 1000 chunks | 5-10 min | 10s | 30-60x |
| 2000 chunks | 15-30 min | 15s | 60-120x |

**实测战绩**（本项目 `lessons_learned_kb` 868 chunks）：
- Full rebuild（BGE 本地）：~5 min
- Incremental rebuild（mtime 复用旧 vec）：**17s**（30x 加速）

---

## 2. 两个工具的分工

### 2.1 `scripts/import_doc.py` — 标准入口（推荐）

**作用**：拷文件 + 重建索引 + 召回验证，三步一体化。
**触发场景**：**任何** "加新文件到 KB" 的任务。

```powershell
# 单文件导入（最常用）
python scripts/import_doc.py "C:\path\to\new_doc.md" --kb rag_engineering

# 整个目录批量导入
python scripts/import_doc.py "C:\path\to\docs\" --kb system_design_kb

# 不重建索引（不推荐, RAG 找不到新文件）
python scripts/import_doc.py some.md --kb my_kb --no-rebuild

# 强制全量重建（默认走增量）
python scripts/import_doc.py some.md --kb my_kb --full-rebuild

# 干跑看计划
python scripts/import_doc.py some.md --kb my_kb --dry-run
```

### 2.2 `scripts/incremental_build.py` — 真增量引擎

**作用**：复用旧 vec，只 embed 改动文件对应的 chunks。
**直接调用**：仅当你想跳过 `import_doc.py` 的"拷文件 + 召回验证"步骤，纯重建索引时。

```powershell
# 直接调（不走 import_doc.py）
python scripts/incremental_build.py data/rag_engineering --out data/rag_engineering/index_sf_bge_m3.pkl --embedder bge-m3
```

### 2.3 ❌ 不要直接调 `build_index.py --mode incremental`

**原因**（2026-09-17 教训）：
- `build_index.py` 的 `--mode incremental` **只跳过 mtime 改变的文件的 *load***，**仍全量 re-embed 所有 chunks**
- 实测 868 chunks → 全量 re-embed 140s 串行（Windows multiprocessing 失败 fallback）
- **不验证召回** → 可能导入失败但你不知道

**结论**：**总是用 `import_doc.py`**，让 wrapper 帮你做对的事。

---

## 3. 三种典型场景

### 场景 A：加新文件（最常见）

```powershell
# 写好新教程 → 一行导入
python scripts/import_doc.py "C:\Users\Administrator\Desktop\my_new_doc.md" --kb rag_engineering
```

**流程**：
```
1. collect_sources: 扫描新文件
2. copy_to_kb: 拷到 data/rag_engineering/my_new_doc.md
3. update_index_md: 00_index.md 末尾追加 "## 最近导入" 段
4. rebuild_index: incremental_build.py (mtime 复用旧 vec)
5. recall_test: 用新文件首 chunk 作 query, top-1 必须命中本文件 (score > 0.3)
```

**实测输出**：
```
📂 [1/5] 收集源文件 ...
  扫描到 1 个文件

📋 [2/5] 检测重复 + 落地到 KB ...
  ✓ copy: my_new_doc.md → my_new_doc.md

📑 [3/5] 更新 00_index.md ...
  ✓ 已更新 00_index.md (1 个新文件)

🔨 [4/5] 重建索引 (incremental_build.py) ...
  [incremental_build.py] 返回码 0, 耗时 17s
      复用 850 vec + re-embed 18 vec (新文件 18 chunks)

✅ [5/5] 召回验证 ...
  top-1 = my_new_doc.md, score=0.876 (>0.3 阈值)
```

### 场景 B：改旧文件（mtime 自动触发）

```powershell
# 用编辑器改完 data/rag_engineering/old_doc.md
# 然后跑 import_doc.py 同一个文件 → mtime 自动检测 → re-embed 旧文件的 chunks
python scripts/import_doc.py "data/rag_engineering/old_doc.md" --kb rag_engineering
```

**注意**：
- 如果改的是 KB 里的文件，传**源路径**（可能不在 KB）也可以，wrapper 会按 basename 找 KB 里的副本
- 如果只改 KB 内部的文件，直接传 KB 路径即可
- mtime 自动触发 re-embed，**不需要** `--full-rebuild`

### 场景 C：删旧文件

```powershell
# 直接删文件, 然后 incremental_build
Remove-Item "data/rag_engineering/deprecated.md"
python scripts/incremental_build.py data/rag_engineering --out data/rag_engineering/index_sf_bge_m3.pkl --embedder bge-m3
```

**注意**：`import_doc.py` 不能直接删文件（设计为 add-only）。删除后调 `incremental_build.py` 会检测到 `removed_files` 并跳过。

---

## 4. 完整实战流程（含排错）

### 4.1 标准 3 步

```powershell
# Step 1: 写好新文件（或修改完旧文件）
code "C:\Users\Administrator\Desktop\生产级多跳 RAG 系统\data\rag_engineering\2026-09-20_my_doc.md"

# Step 2: 走 import_doc.py 标准 wrapper
python scripts/import_doc.py "C:\Users\Administrator\Desktop\生产级多跳 RAG 系统\data\rag_engineering\2026-09-20_my_doc.md" --kb rag_engineering

# Step 3: 验证召回（自动跑, 不需要手动）
python scripts/ask.py --kb rag_engineering --query "my doc 里的核心概念"
```

### 4.2 排错 Checklist

| 现象 | 原因 | 解法 |
|---|---|---|
| `❌ KB 目录不存在` | `data/<kb>/` 没建 | 先 `mkdir data/<kb>/` |
| `[skip] 已存在` | 文件已导入 | 改名 (`xxx_v2.md`) 或 `os.remove()` 后再导 |
| `top-1 不在新文件集` | recall 失败 | 改 query 重试, 或检查文件内容相关性 |
| `TIMEOUT after 1800s` | build 超时 | 默认 30 min, 大 KB 改 `BUILD_INDEX_TIMEOUT=3600` |
| `[+] filename (chunks 变化 X→Y)` | 改了 chunk_size | 预期行为, re-embed 整个文件 |
| `expected single instance, got 0` | token embedder 没 fit | 重新跑 full rebuild |
| PowerShell 中文乱码 | GBK stdout | python `sys.stdout.reconfigure(encoding="utf-8")` 已内置 |

### 4.3 何时必须 full rebuild（不走增量）

| 场景 | 原因 |
|---|---|
| **chunk_size 改了** | 旧 chunks 边界失效, 全 re-embed |
| **embedder 换了**（BGE → SF bge-m3） | 旧 vec 维度不同, 不能复用 |
| **首次构建（pkl 不存在）** | 没旧 vec 可复用 |
| **loader.py 大改**（如切分逻辑变化） | 旧 chunks 错位 |

`import_doc.py` 自动检测 pkl 是否存在 → 不存在自动 full rebuild。chunk_size / embedder 改 → 需手动 `--full-rebuild`。

---

## 5. 性能基线（实测）

### 5.1 增量 vs 全量对比

| KB | Chunks | 全量 | 增量 | 倍数 |
|---|---|---|---|---|
| `rag_engineering` | 473 | 4 min | **8s** | 30x |
| `lessons_learned_kb` | 868 | 5 min | **17s** | 18x |
| `marx_engels_kb` | ~600 | 3 min | 12s | 15x |

### 5.2 embedder 选择

| Embedder | 速度 (本地 CPU) | 成本 | 适用 |
|---|---|---|---|
| `token` (TokenSetEmbedder) | **极快** (0 安装) | 0 | 小 KB / 测试 |
| `bge` (BGE 本地) | 慢 (2 chunks/s) | 0 | 不推荐, 25 min+ 易卡死 |
| `bge-m3` (SF API) | **极快** (74 chunks/s) | **免费** (SF 促销) | ⭐推荐 |

**默认 `import_doc.py` 走 `bge-m3`**，配 `SILICONFLOW_API_KEY` 环境变量。

---

## 6. 跨 KB 场景

### 6.1 同一文件入多个 KB（罕见）

不直接支持。做法：
```powershell
# 拷到多个 KB
Copy-Item "new_doc.md" "data/kb_a/"
Copy-Item "new_doc.md" "data/kb_b/"

# 各自增量 rebuild
python scripts/incremental_build.py data/kb_a --out data/kb_a/index.pkl --embedder bge-m3
python scripts/incremental_build.py data/kb_b --out data/kb_b/index.pkl --embedder bge-m3
```

### 6.2 跨 KB 检索

`ask.py` 支持 `--index-path` 直接指定 pkl：
```powershell
python scripts/ask.py --index-path data/rag_engineering/index_sf_bge_m3.pkl --query "..."
# vs python scripts/ask.py --kb rag_engineering --query "..."  (慢, 7min for 903 chunks)
```

**实测**：rag_engineering 473 chunks 用 `--index-path` 秒级, `--kb` 模式要 4 min。

---

## 7. 自动化集成（进阶）

### 7.1 Git pre-commit hook

```bash
# .git/hooks/pre-commit
#!/bin/bash
# 提交前自动增量 rebuild 改动的 KB
changed_files=$(git diff --cached --name-only | grep "data/.*\.md$")
if [ -n "$changed_files" ]; then
    python scripts/import_doc.py "$changed_files" --kb rag_engineering || exit 1
fi
```

### 7.2 定时任务（每日重建）

```powershell
# Windows Task Scheduler 每日 06:00 重建
$action = New-ScheduledTaskAction -Execute "python" -Argument "scripts/rebuild_all_kbs.py"
$trigger = New-ScheduledTaskTrigger -Daily -At "06:00AM"
Register-ScheduledTask -TaskName "RAG Daily Rebuild" -Action $action -Trigger $trigger
```

---

## 8. 常见坑（已踩过）

### 坑 1：同名文件 skip bug
**症状**：覆盖 KB 里的同名文件, `import_doc.py` 检测到"已存在" → skip → KB 没更新。
**解法**：
- 改名（`xxx_v2.md`）
- 或 `os.remove()` 旧文件后重导

### 坑 2：token embedder 没 fit
**症状**：`incremental_build.py` 用 `token` embedder 报 `expected single instance, got 0`。
**原因**：`TokenSetEmbedder` 需要 `fit()` 一次才能 embed 查询。
**解法**：用 `bge-m3` 替代（默认）。

### 坑 3：PowerShell 60s timeout
**症状**：bash 工具杀 wrapper, build_index 子进程跟着死。
**解法**：`import_doc.py` 用 `subprocess.run(timeout=1800)`（已内置 30 min）。

### 坑 4：跨平台 path 编码
**症状**：PowerShell GBK stdout 中文乱码, file path 读不到。
**解法**：`import_doc.py` 已 `sys.stdout.reconfigure(encoding="utf-8")`。

### 坑 5：incremental 没减少 chunks
**症状**：删了文件但 chunks 数没变。
**原因**：incremental_build 按 source 文件名比对, 删的文件 → "removed" 但**新 docs list 里没它**, 所以 chunks 减了, 但 `_index_mtime.json` 里还在。
**解法**：删文件后跑 `incremental_build.py` (会自动清), 或跑 `--full-rebuild`。

---

## 9. 实战参考命令（贴贴板）

```powershell
# 加一个新教程到 rag_engineering
python scripts/import_doc.py "C:\Users\Administrator\Desktop\new_doc.md" --kb rag_engineering

# 改了一个旧文件, mtime 触发增量
python scripts/import_doc.py "data/rag_engineering/old.md" --kb rag_engineering

# 删除 + 增量 rebuild
Remove-Item "data/rag_engineering/deprecated.md"
python scripts/incremental_build.py data/rag_engineering --out data/rag_engineering/index_sf_bge_m3.pkl --embedder bge-m3

# 全量重建 (chunk_size 改了 / embedder 换了)
python scripts/import_doc.py "data/rag_engineering\" --kb rag_engineering --full-rebuild

# 批量回灌多个 KB
foreach ($kb in @('rag_engineering', 'lessons_learned_kb', 'marx_engels_kb')) {
    python scripts/incremental_build.py "data/$kb" --out "data/$kb/index_sf_bge_m3.pkl" --embedder bge-m3
}

# 验证召回（任何 KB 都行）
python scripts/ask.py --index-path data/rag_engineering/index_sf_bge_m3.pkl --query "RAG 4 阶段架构" --defense
```

---

## 10. 关键 takeaway

| 原则 | 怎么做 |
|---|---|
| **总是走 import_doc.py** | 让 wrapper 帮你做对的事 |
| **增量 ≠ 0 代价** | 仍需 8-17s 启动 + embed, 但比全量 30x 快 |
| **mtime 是触发器** | 编辑文件后 mtime 自动变 → incremental 自动 re-embed |
| **chunk 数变化 ≠ mtime 变** | loader 改后即使文件 mtime 没变, chunks 数变了也会 re-embed |
| **full rebuild 慎用** | 只在 chunk_size/embedder/loader 大改时用 |
| **召回验证不可省** | top-1 必须命中新文件 (score > 0.3), 否则不认为导入成功 |

---

**相关教程**：
- `2026-09-18_RAG系统体检+修复实战总结.md` — 教训 #26-#27 (detached 进程 + 不刷新陷阱)
- `Jev_Like_国内可行方案_SiliconFlow_Qwen_2026-09-19.md` — 决策 API 替代 LLM
- `2026-09-20_RAG集成JevLike决策教程.md` — Router/Verifier/Citation 集成

**底层工具源码**：
- `scripts/import_doc.py` (500+ 行, 完整 5 步流程)
- `scripts/incremental_build.py` (270+ 行, 真增量引擎)
- `scripts/build_index.py` (full rebuild, 不要直接用 `--mode incremental`)